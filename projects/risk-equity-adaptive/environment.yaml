# projects/risk-equity-adaptive/environment.yaml

name: conda_envs
channels:
  - defaults
  - conda-forge
  - anaconda
dependencies:
  # --- Conda packages ---
  - python
  - pandas
  - networkx
  - matplotlib
  - pydantic
  - ruff
  - mypy

  # --- Pip packages ---
  - pip:
      - contextily
      - geopandas
      - shapely
